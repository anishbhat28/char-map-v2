Add experiments/audit_severe_h4_full_timeline.py to the current repo.

Run:
python -m experiments.audit_severe_h4_full_timeline

It checks every valid H=4 severe-OOD timestep and reports state MSE, graph mismatch,
exact-graph fraction, and all mismatch timesteps.
