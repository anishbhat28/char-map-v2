Add experiments/multiseed_breakdown_validation.py and run:
python -m experiments.multiseed_breakdown_validation

It runs the three selected regimes across seeds 50000-50004 and saves raw results after every case.
